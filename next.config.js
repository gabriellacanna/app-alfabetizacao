/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['placeholder.com'], // adicione aqui os domínios de imagens que você usa
  },
}

module.exports = nextConfig
